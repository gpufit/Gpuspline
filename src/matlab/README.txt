Matlab binding for the [Gpuspline library](https://github.com/gpufit/Gpuspline) (calculation of multidimensional cubic splines)-
Requirements

- Windows
- Matlab

Installation

An installation is not necessary. However, this path must be part of the Matlab path. Use `addpath` if necessary.

Examples

See /examples/matlab folder.